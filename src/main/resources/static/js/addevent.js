/*
var newEvent = document.getElementById("testJsButton");

var userEvents = JSON.parse(document.getElementById("userEvents").innerText);
console.log(userEvents);

newEvent.onclick = function(){
console.log("test");
//console.log(jsonEvents);

for(var i = 0; i < userEvents.length; i++) {

    var date = new Date(userEvents[i].eventDate);
    console.log("event date: " + date);

    var obj = [i];
   // console.log("inside for");
    if(userEvents[i].eventStatus === "FUTURE") {
        console.log(userEvents[i].eventStatus)
    }

}
}

 */